1. The bug was that the input values were acting like strings so when it tried to add them it just combined them instead of doing actual math. It’s cause input values are always strings.

2. To fix it you just have to change the inputs into numbers before adding them. That way it adds the values like normal numbers instead of putting them together like text.

